export const environment = {
  production: false,
  // Your Spring Cloud Gateway's local development URL
  // Assuming Gateway runs on 8080 and its routes start with /api
  apiUrl: 'http://localhost:8080/api'
};
